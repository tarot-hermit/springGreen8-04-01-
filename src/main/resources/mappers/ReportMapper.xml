<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.springGreen8.dao.ReportDAO">

    <resultMap id="reportResultMap" type="com.spring.springGreen8.vo.ReportVO">
        <id property="reportId" column="report_id"/>
        <result property="reporterMid" column="reporter_mid"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="reason" column="reason"/>
        <result property="status" column="status"/>
        <result property="regDate" column="reg_date"/>
    </resultMap>

    <insert id="insertReport">
        INSERT INTO report (reporter_mid, target_type, target_id, reason)
        VALUES (#{reporterMid}, #{targetType}, #{targetId}, #{reason})
    </insert>

    <select id="checkDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM report
        WHERE reporter_mid = #{reporterMid}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <select id="selectAllReports" resultMap="reportResultMap">
        SELECT report_id, reporter_mid, target_type, target_id,
               reason, status, reg_date
        FROM report
        ORDER BY reg_date DESC
    </select>

    <update id="updateStatus">
        UPDATE report
        SET status = #{status}
        WHERE report_id = #{reportId}
    </update>

    <delete id="deleteReportsByReporterMid">
        DELETE FROM report
        WHERE reporter_mid = #{reporterMid}
    </delete>

</mapper>
