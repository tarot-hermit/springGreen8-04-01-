<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.springGreen8.dao.CollectionDAO">

    <resultMap id="collectionResultMap" type="com.spring.springGreen8.vo.CollectionVO">
        <id property="collectionId" column="collection_id"/>
        <result property="mid" column="mid"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="isPublic" column="is_public"/>
        <result property="regDate" column="reg_date"/>
        <result property="updateDate" column="update_date"/>
        <result property="movieCount" column="movie_count"/>
        <result property="inCollection" column="in_collection"/>
    </resultMap>

    <insert id="insertCollection" useGeneratedKeys="true" keyProperty="collectionId">
        INSERT INTO collection (mid, title, description, is_public)
        VALUES (#{mid}, #{title}, #{description}, #{isPublic})
    </insert>

    <select id="selectMyCollections" resultMap="collectionResultMap">
        SELECT c.collection_id, c.mid, c.title, c.description,
               c.is_public, c.reg_date, c.update_date,
               COUNT(cm.movie_id) AS movie_count,
               <choose>
                   <when test="movieId != null">
                       MAX(CASE WHEN cm.movie_id = #{movieId} THEN 1 ELSE 0 END) AS in_collection
                   </when>
                   <otherwise>
                       0 AS in_collection
                   </otherwise>
               </choose>
        FROM collection c
        LEFT JOIN collection_movie cm ON c.collection_id = cm.collection_id
        WHERE c.mid = #{mid}
        GROUP BY c.collection_id
        ORDER BY c.update_date DESC
    </select>

    <select id="selectPublicCollections" resultMap="collectionResultMap">
        SELECT c.collection_id, c.mid, c.title, c.description,
               c.is_public, c.reg_date, c.update_date,
               COUNT(cm.movie_id) AS movie_count,
               0 AS in_collection
        FROM collection c
        LEFT JOIN collection_movie cm ON c.collection_id = cm.collection_id
        WHERE c.is_public = 1
        GROUP BY c.collection_id
        ORDER BY c.update_date DESC
        LIMIT 50
    </select>

    <select id="selectCollectionById" resultMap="collectionResultMap">
        SELECT c.collection_id, c.mid, c.title, c.description,
               c.is_public, c.reg_date, c.update_date,
               COUNT(cm.movie_id) AS movie_count,
               0 AS in_collection
        FROM collection c
        LEFT JOIN collection_movie cm ON c.collection_id = cm.collection_id
        WHERE c.collection_id = #{collectionId}
        GROUP BY c.collection_id
    </select>

    <update id="updateCollection">
        UPDATE collection
        SET title = #{title},
            description = #{description},
            is_public = #{isPublic}
        WHERE collection_id = #{collectionId}
          AND mid = #{mid}
    </update>

    <delete id="deleteCollection">
        DELETE FROM collection
        WHERE collection_id = #{collectionId}
          AND mid = #{mid}
    </delete>

    <delete id="deleteCollectionsByMid">
        DELETE FROM collection
        WHERE mid = #{mid}
    </delete>

    <insert id="insertCollectionMovie">
        INSERT IGNORE INTO collection_movie (collection_id, movie_id)
        VALUES (#{collectionId}, #{movieId})
    </insert>

    <delete id="deleteCollectionMovie">
        DELETE FROM collection_movie
        WHERE collection_id = #{collectionId}
          AND movie_id = #{movieId}
    </delete>

    <select id="selectMovieIdsByCollection" resultType="int">
        SELECT movie_id
        FROM collection_movie
        WHERE collection_id = #{collectionId}
    </select>

    <select id="checkCollectionMovie" resultType="int">
        SELECT COUNT(*)
        FROM collection_movie
        WHERE collection_id = #{collectionId}
          AND movie_id = #{movieId}
    </select>

</mapper>